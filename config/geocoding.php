<?php

return [
    // Версия логики парсинга ответов геокодеров.
    // УВЕЛИЧИВАЙ на 1 при любом изменении того, как парсится ответ
    // Nominatim/Photon (queryNominatim/queryPhoton в GeocodeAddressJob),
    // чтобы старые (потенциально ошибочные) закэшированные результаты
    // автоматически перестали использоваться — без ручной чистки Redis.
    'cache_version' => 1,

    // TTL для успешно найденных адресов (геокодер вернул координаты)
    'cache_ttl_found_days' => 30,

    // TTL для отрицательных результатов (геокодер честно ответил "не найдено")
    // Короткий специально: если это была временная сетевая проблема,
    // а не то, что адреса реально нет в OSM — не хотим "хоронить" адрес на месяц.
    'cache_ttl_not_found_hours' => 6,
];
